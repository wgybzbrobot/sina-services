<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.pp.sina.dao.bozhus.BozhuLibraryDao">

	<!-- 获取博主库中所有博主Uid -->
	<select id="getBozhuUids" resultType="long">
	    SELECT `username` FROM `bozhu`;
	</select>

</mapper>